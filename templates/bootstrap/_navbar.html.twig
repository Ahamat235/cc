<nav class="navbar navbar-dark bg-primary">
    <!-- Navbar content -->
    <div class="container">
        <a class="navbar-nav mr-auto" href="{{ path('app_atelier_index') }}">
            <i class="bi-house" style="font-size: 2rem; color: black;"></i>
        </a>
        {%  if app.user %}
            {% if 'ROLE_APPRENTI' in app.user.roles %}
                <a class="navbar-brand mr-2" type="button" href="{{ path('mesinscriptions') }}">Mes inscriptions</a>
            {% endif %}
        <a class="navbar-brand ml-8" href="{{ path('app_atelier_new') }}">
            <i class="bi-plus" style="font-size: 2rem; color: black;"></i>
        </a>

        <a class="navbar-nav">Register
            <a class="navbar-brand" type="button" href="{{ path('app_register') }}">Register</a>
        </a>

        <a class="navbar-nav">logout
            <a class="navbar-brand" type="button" href="{{ path('app_logout') }}">logout</a>
        </a>

    {% else %}

        <a class="navbar-nav">Register
            <a class="navbar-brand" type="button" href="{{ path('app_register') }}">Register</a>
        </a>

        <a class="navbar-nav">login
            <a class="navbar-brand" type="button" href="{{ path('app_login') }}">login</a>
        </a>

    {%  endif %}
    </div>
</nav>
